# Open findings register

Things measured, recorded, and **not fixed** — with no line item in any stage
of [NSOT_PLAN.md](NSOT_PLAN.md).

**Why this file exists.** Each of these was written into `CLAUDE.md` or a
stage document at the moment it was found, in prose, beside the thing it was
found next to. That is the right place to explain *why* something is true and
the wrong place to keep a list: prose accumulates invisibly, and the only way
to know what is outstanding was to have been present when each one was
recorded. The register is the list; the prose stays where it is and is linked.

**Rules.** An item leaves this file by being fixed, by being scheduled into a
stage, or by being closed with a reason. It does not leave by being
forgotten. Anything recorded as *"not applied"*, *"noted, not yet
addressed"*, *"recorded as a gap"* or *"left open"* belongs here on the same
day it is written.

**A register is only current if the reader checks the head.** A stale read
of a maintained file cannot be told apart from a stale file. On 2026-09-25 this
register was read at `1b9b4d3` while the repository was four commits ahead,
and three items already closed were raised again as open. That is the failure
this file exists to prevent, arriving through the reader rather than the
document. Before acting on an item, run `git log -1 -- docs/OPEN_FINDINGS.md`
and compare it with the head you are reading.

Status: **open** unless stated. Last reviewed 2026-09-26.

> **No live thread.** C3 closed, and the positive control re-run on `bbf3d8e`
> passed (§23).
>
> **C3 is closed (2026-09-25, [PHASE2_DHCP.md](PHASE2_DHCP.md) §23).** The
> recorder did **not** miss the write: `data/netbox_modified.json` holds
> `r1 comments … TEST → …` at `06:34:40Z`, written 0.18 s after NetBox's
> `last_updated`. The silent syncs since the churn fixes stand as evidence
> again. How the entry came to be read as absent was not determined.

---

## A. Data integrity — NetBox

| # | Finding | Kind | Where recorded |
|---|---|---|---|
| A2 | **The census records identity, not assignment.** `_identity()` is `id:display`, so an object moved between parents is invisible to `--compare` — which is exactly the 2026-09-24 address incident. The modification record now catches NMAS's own moves; a move by anything else still is not seen. | build | [PHASE2_DHCP.md](PHASE2_DHCP.md) §12 |
| A3 | **Nine of ten devices are outside Remove's provenance.** Both halves — the `nmas-managed` tag and `netbox_created_ids.json` — arrive in Phase 0 (2026-09-20); the sync dates from 2026-04-22. The nine reference devices carry neither, so a provenance-based Remove can act on r6 and is blind to the rest. Safe, and it means the teardown mechanism has never been proven against them and by construction never can be. Adopting them is a deliberate act. | decide, then build | [PHASE2_DHCP.md](PHASE2_DHCP.md) §16 |
| A4 | **NetBox records every device's platform as `ios`, the C8000v routers included, and NMAS wrote it.** Measured 2026-09-25: r1–r6 (C8000v, IOS-XE) and s1–s4 (vIOS-L2) are all `ios`. The NSoT manifest has them right. **The writer is `_parse_facts_from_config()`** (`netbox_client.py`, used by the golden-driven import). It **defaults to `"IOS"`** and switches to `IOS-XE` only if the config TEXT contains "IOS XE" or "IOS-XE". **A captured golden contains neither**: the live `golden/r2.cfg` has 0 occurrences. So every router falls back to the default, and `_ensure_platform()` slugs it `ios`. **The fleet fixtures each contain the string once**, so any test of this path passes against a fixture while the live store fails it: a fixture that cannot exhibit the case, again. `platform` is in the sync's update allowlist, so **every sync re-asserts `ios`**. A human correction in NetBox would be reverted on the next sync, and only the modification record would show it. A text-sniffing default written as a fact: the fix is to take the platform from the manifest's dialect through `platform.py`, the one translation, never from config text. Consequences: a NetBox-sourced list cannot resolve any device's platform (`ios` is not a slug `platform.py` maps), and anything keyed on NetBox's platform gives routers switch behaviour. Found because the heartbeat window has to be per platform. | build | NSOT_PLAN.md P.1 (step 4, r2) |

## B. Secrets and audit surfaces

| # | Finding | Kind | Where recorded |
|---|---|---|---|
| B1 | **`skipped_drifted` returns a whole device configuration in an API response.** Unredacted, on a JSON route. The diagnostic value was already removed (it now carries both hashes); the config remains. | build | CLAUDE.md, *"A diagnostic that dumps the artefact but not the comparison"* |
| B2 | **A credential rotation leaves no durable record.** `persist()` is reached only from two scripts, and nothing writes an audit file, a run log or anything in `data/`. Whether the Oxidized stages have been failing since the 2026-09-23 settings erasure **is not knowable from the repository**. **FIRST MEASURED COST (2026-09-26, B13):** s1's rotation of an EXPOSED credential. The device rotated, but the persistence chain produced no sync, and s1's boot file kept the exposed value for about 8.5 minutes until the timer wrote it. Which stage stopped the chain could not be established from anything durable; it was in one terminal's scrollback. A record written at each stage, with names and outcomes and never values, is the natural companion of P.3 step 7's terminal audit. **Measured a second time in the same incident:** the stage lines were also needed to close B13, and were gone. The rotation half is now in P.3 step 12 (B15). | build | CLAUDE.md, *"`oxidized_reload` is stage 2"* |
| B3 | **Orphaned credential overrides are reported and never removed.** `nmas-credential-overrides` names them deliberately — an override may be a break-glass credential — but nothing closes the loop, so the store grows. The `''` key from before the DHCP key fix is still there unless cleared by hand. | decide, then build | CLAUDE.md, *"`credential_profiles.json` is a secret store with no expiry"* |
| B8 | **The NetBox backups' decryption key exists only on the operator's laptop.** Its gpg keyring and a `paperkey` file (`0600`, restore-tested) sit on one disk; there was no printer for the paper copy (2026-09-25). **This is not "no backup". It is "backups that survive and cannot be read".** The copies on the VM are plain (`/var/lib/nmas-netbox`, `0700`); only the copies shipped to Proxmox and B2 are gpg-encrypted to this key. So, by what is lost: **(1)** **the laptop alone**: no NetBox data is lost. The off-VM copies taken so far become permanently unreadable, and a new keypair covers future ones; **(2)** **the laptop and the NMAS VM, with the Proxmox host intact**: recoverable from B6's nightly image, which contains the VM's plain copies. Up to a day is lost, and the Proxmox copies are unreadable; **(3)** **the laptop and the Proxmox host**: the B2 dailies are the only copies left, and **nothing can read them**. This is the scenario the off-host copy exists for, and it fails in it. **The same laptop also holds the break-glass record and, in its password manager, that record's passphrase**, so it is also the only OFF-HOST copy of `data/key.key` (B5's escrow). In case (3) the key goes with it: the VM and its image are on the lost host. One laptop is the single off-host holder of both keys. **Closes when** the gpg secret key, and the break-glass record with it, exist somewhere that does not fail with this laptop: a USB copy kept elsewhere, or a password-manager entry that syncs off it. Test it the way the paper copy was tested: restore, then decrypt a real `.tar.gpg`. Not blocking P.2 step 2. | operator's step | docs/NETBOX_BACKUP.md §1 |
| B9 | **CONFIRMED 2026-09-26: the off-box B2 key can destroy the backups it writes, without `deleteFiles`.** Run by the operator: `rclone delete -vv --include b2probe.txt` printed `Deleted` with no refusal, the normal listing was empty, and `--b2-versions` held the hidden version. `writeFiles` allows a HIDE, and the lifecycle deletes hidden versions a day later, so a compromised NMAS erases every off-box copy within 24 h. The first test (a single-file path: exit 0, file still listed) passed while proving nothing, because rclone's single-file check needs `readFiles`, so the file was never touched. **Fix: B2 Object Lock, Governance mode, default retention 16 days: the lifecycle's hide-after (15) plus its hide-to-delete (1)**, so no hide can delete anything sooner than the lifecycle would have. (First recorded as 15, which was a day short; corrected 2026-09-26) (docs/NETBOX_BACKUP.md §4). The semantics are from B2's documentation as understood here, and a five-step probe plan measures them before real data depends on them. **Closes when** step 4 of that plan shows a hidden LOCKED version surviving the lifecycle, and step 5 shows a governance bypass works. | operator's step (measure, then enable) | docs/NETBOX_BACKUP.md §4 |
| B10 | **Nothing checks that the bucket's lock retention covers its lifecycle window.** **Decided 2026-09-26 (operator): a read-only key**, scoped to the bucket, which is ALSO the retrieval key (docs/NETBOX_BACKUP.md §6): one key, on the laptop to fetch and on the NMAS to check. It may live on the NMAS because everything it can read, the NMAS already holds unencrypted. The key exists (2026-09-26), and its capabilities include `readBucketLifecycleRules`, so the lifecycle half is readable. Whether it can read the Object Lock retention is unknown until the check is built against the real response. It also passed retrieval end to end (docs/NETBOX_BACKUP.md 6f), and its attempt to hide was refused with a 401. It cannot write, so it cannot undermine what it checks, and a compromised NMAS can use it only to look. A manual check in the B2 UI was rejected: it is done once and never again, and this pair is exactly what drifts when one is changed and the other forgotten. **The property to check is not "both are 15"** but **lock retention >= daysFromUploadingToHiding + daysFromHidingToDeleting**. Shorter fails, because files become deletable before the lifecycle removes them: the hole re-opening silently. Longer warns, because the lock has silently become the retention. Equal passes. **Build after** the lock is enabled and the key exists, against the real `b2_list_buckets` response: building against documented field shapes is how B6's first read reported `never` for two VMs that had images. The new key goes into `nmas-check-secret-storage`'s declared list. | build | docs/NETBOX_BACKUP.md §4 |

## C. Tooling that reports wrongly

| # | Finding | Kind | Where recorded |
|---|---|---|---|
| C6 | **A seeded artefact never learns that its source changed, and nothing can say which copies are stale.** Code creates it once, never overwrites it (correctly: it must not clobber a local edit), and never compares it again. The class, not the instance. Surveyed 2026-09-25, **six kinds**: **(1)** the per-list template library: `_common.j2`, `cisco_ios/base.j2`, `cisco_iosxe/base.j2`, `bindings.yml`. *Measured on `default`:* `_common.j2` equals the shipped version at `4771a4c`, carried in by hand as `c8e07cc`, so its only divergence is P.1's 9 lines; the two `base.j2` are current and unedited. **(2)** NetBox objects NMAS creates with **code-defined** attributes and never updates: the `os_version` custom field (label, type, object types) and 4 tag definitions (colours, including `nmas-managed`). A custom-field failure is logged at **DEBUG** (`netbox_client.py:659`), so a stale definition fails invisibly. **(3)** Settings: the v1 migration seeded every key, and the live file was reseeded with 107 defaults on 2026-09-23, so every key there holds that day's default and a later default change never reaches it. `nmas-settings-diff` shows that a value differs, not why. **(4)** Host installs from `deploy/`: 4 systemd units, the rsyslog conf, the Grafana rules. Nothing compares the installed copy with the repo. **(5)** The launch-patch copies on the clab host (`labs/*/patches`), per-lab by design; the guard checks their call site, not their version. **(6)** The legacy `config_git` `.gitignore`: written once, never topped up. `config_repo`'s `ensure_repo_hygiene()` tops up on every access and is the right shape. **Not in the class:** the NDM config template in NetBox, which is re-asserted every sync (the opposite property: it overwrites a NetBox-side edit, and the modification record now records that). **Can anything answer "which lists run a stale seed"? No, for any of the six.** But for (1) the answer exists in data already held: each list's repo has its seed commit, and hashing the network copy against every shipped version in git history classifies each file as *current*, *stale* (equals an older shipped version), *edited* (equals none) or *edited and stale*. That is how `default` was measured above, with no new record. **Answers BUILT 2026-09-25 for (1) and (2):** `templates_repo.seed_status()` (GET `/templates/seed_status`) classifies each file as current / stale / edited / edited_and_stale from the two git histories, and **edited never counts as a defect**. Its first live run was WRONG and its tests passed: a relative path doubled inside `git -C` and every file read `edited_and_stale`, because every fixture used absolute paths. Fixed, re-measured: `_common.j2` stale at `4771a4c`, both `base.j2` current, which agrees with the independent hash measurement. `netbox_client.seeded_spec_status()` compares the custom field and 8 tag specs with NetBox. Live: 7 current, 2 absent (`mpls`, `eigrp`, which no device runs), none differ. `scripts/nmas-seed-status` reports both. **Still unanswered: (4) host installs, (5) lab launch-patch copies, (6) the legacy `.gitignore`.** (3) is split out as C7, and the DEBUG-logging class found beside (2) is C8. | build | this file, 2026-09-25 (P.1) |
| C7 | **The 2026-09-23 settings erasure is permanent in a way it had not been named: every non-empty default is FROZEN too.** The erasure's reseed wrote all 107 keys, so this deployment holds that day's default for every key, and a later change to any default never reaches it. `GUARD_GATING_EMPTY_DEFAULTS` covered the keys that default to empty (a guard that silently refuses). This is the rest of them: **nothing distinguishes a value somebody chose from a default that has since moved, and it is silent in both directions.** A moved default does not arrive, and the frozen value reads as a choice (`origin: file`, and `nmas-settings-diff` lists it as *differs*). It is worse than the guards case because nothing refuses. **The same classifier would answer it.** `settings_schema.DEFAULTS` has a git history, so each written value is either today's default (*current*), a past default and not today's (*frozen at an old default*), or no default ever (*chosen*), with *ambiguous* where a chosen value happens to equal a past default. That is not built. | build | this file, 2026-09-25 |
| C8 | **Twenty NetBox write failures are logged at DEBUG.** Surveyed while fixing C6 item 2: `netbox_client.py` has 22 write paths whose failure goes to `log.debug`, including tunnel, termination and cable creates, platform, VRF and VLAN upserts, device tag updates, prefix roles, secondary and IPv6 addresses, LAG wiring, static routes, primary IP fallbacks and `_nb_delete`. The sync then reports `failed=0`. Two are fixed (the custom field and the two tag ensures now warn and reach the sync's `notes` through `_ensure_failed()`); **the other 20 are the same shape and unfixed.** `_ensure_failed()` is the channel they should use: warn, and be named in the summary a person reads. | build | this file, 2026-09-25 |
| C9 | **The vIOS clock runs at about 75% speed, and reads ~16 hours behind.** Measured on s4, 2026-09-25: 300.0 s of device time is 391–404 s of real time over five intervals. So **every timestamp a switch writes is wrong**: in its syslog lines, its `show logging`, and its captured configs' `Last configuration change`. NTP is configured (`ntp server 10.255.1.10`) and is plainly not keeping s4's clock. Whether NMAS's host serves NTP, and whether any slewing could correct a 25% rate error, is not measured. The heartbeat is safe because its window is per platform; **anything that compares a switch's own timestamps with wall time is not.** | decide, then build | NSOT_PLAN.md P.1 (step 4, s4) |
| C10 | **A batch deploy's circuit breaker counts VERIFY failures and not PUSH failures.** `run_batch()` records a failure towards the breaker only when `outcome == failed and stage == "verify"` (`deploy.py`). A push rejected on every device, such as a command an image does not accept, is therefore attempted and rolled back on **every** device in the batch, never stopped. The breaker's own docstring says repeated failures mean "something systemic… and continuing turns one mistake into nine", and a platform-wide push rejection is the most systemic failure there is. Named while answering whether the P.1 six could be one batch (2026-09-25). The risk there is low, because s4 and r2 had proven both platforms first. | build | this file, 2026-09-25 |
| C12 | **P.1 is add-only: the deploy path can put the heartbeat applet on a device and cannot take it off.** Merge-only never negates. Reverting intent and deploying would send `logging trap critical`, and the applet would appear as a removal warning while nothing is sent: the `passive-interface` limit again. The rollback path does generate `no event manager applet NMAS-HEARTBEAT`, but only to undo a failed push. Removing the block from a device on purpose, as for r5 leaving management, is a hand change or Mode B (not built). | decide | NSOT_PLAN.md P.1 (r5) |
| C13 | **Syslog lines name a device only if the NMAS host's hand-kept `/etc/hosts` knows it.** Measured 2026-09-25: `/etc/hosts` (last modified 2026-09-07) lists r1–r5 and s1–s4 and not r6, so rsyslog's reverse lookup writes `10.255.1.16` in the hostname field of r6's lines where every other device gets its name. The heartbeat rules are **unaffected by design and pinned**: they key on the device's own `origin-id` field (`\s<host>:\s`), never rsyslog's. Anything keyed on the rsyslog field, such as a Grafana panel or a Loki label, would treat r6 differently, silently. The same shape as `ROUTERS="r1 r2 r3 r4 r5"`: a list complete when written and wrong for every device since. Fix by not depending on it, or have onboarding own the entry. | decide | NSOT_PLAN.md P.1 (r6's first heartbeat) |
| C14 | **A scheduled job can fail for a day and nothing says so.** Measured 2026-09-25: `clab-sync.service` failed **72 consecutive runs** (every 30 min from 2026-09-24 08:40; last success 08:10) with `nmas-clab-targets: command not found`. The helper was a `~/bin` symlink on the login PATH and not systemd's. The refusal was correct and went into a journal nobody read, and r6's startup config fell a day behind its branch-site config. Had the clab host rebooted, r6 would have booted management-only. **Fixed at the cause:** the sanitiser resolves its helpers beside its own real path, refuses by name when one is missing, and is deployed as a symlink to `scripts/oxidized-to-config.sh`. The deployed copy had also diverged from the repository's (C6 (4)). **The class is BUILT and not yet SURFACED:** `modules/job_health.py` (GET `/jobs/health`, `scripts/nmas-jobs`) reads systemd and the journal per declared job and reports ok / failing (the cause line, the streak) / stale / never_succeeded / not_installed (never ok: systemd reports `Result=success` for a unit that does not exist, measured) / unknown. Live: "0 of 3 job(s) ok". It is visible when something a person looks at shows it, which is 7.2's service-status bar. | build (surface) | NSOT_PLAN.md P.1 (r5 retirement, step 2) |
| C17 | **NMAS's own Loki and Grafana integrations are unconfigured on the deployment.** Measured 2026-09-25: `loki_url` and `grafana_url` are both empty, so their Integration cards say "not configured" while both services run on the same host. Tools that need them take an explicit URL instead (`nmas-heartbeat-rules --loki-url`), and refuse rather than guess `localhost` when it is absent. Whether this is another casualty of the 2026-09-23 settings erasure (C7) is not established. | decide | NSOT_PLAN.md P.1 (step 5) |
| C25 | **One device without a manifest identity makes Save All commit NOTHING for the whole list, and Discover Subnet creates exactly such devices.** `save_golden(allow_new=False)` returns from inside its loop at the first device it cannot resolve (`modules/nsot/repo.py` in `save_golden`), so Save All answers 500 and no capture is committed. `/add_discovered_devices` writes CSV rows only, with no identity. That refusal also says *"Only those two callers pass allow_new=True"*, and no production caller does (`test_identity_minting` asserts it): a message describing the code as it was. Separately, `/add` writes the manifest BEFORE `save_device`, which refuses on a NetBox-sourced list, leaving an entry with no row; and it hard-codes `device_type: cisco_ios`. Confirmed in code 2026-09-26. **LIVE CHECK (2026-09-26, read-only on the host): Default is complete, and Save All never captured less than it said.** 9 CSV rows, 9 manifest entries, no row without an identity. The app log holds 24 of the `save_golden` refusals, and all 24 are TEST FIXTURES (TEST-NET 203.0.113.x addresses, a list called `Lab`, devices called `BRAND-NEW` and `never-seen`), written by a suite run in the live checkout on 2026-09-23 (C26). The refusal is also loud by construction: nothing is committed and the route answers 500. So the failure mode is "Save All does nothing", never "Save All does less and says it did all". The four `save_all` commits name only the devices that CHANGED, so their short `Devices:` lists are not evidence of missed captures. | build | [NSOT_FEATURE_AUDIT.md](NSOT_FEATURE_AUDIT.md) section 8 |

## D. Interface and input surfaces

| # | Finding | Kind | Where recorded |
|---|---|---|---|
| D1 | **`domain` is read by `_plan_args()` and no form sends it.** Recorded as *a gap under exemption* by `test_server_reads_nothing_the_form_cannot_send.py` rather than a clean pass — unlike `secret` and `source_kind`, there is no reason a person could not set it. | build | CLAUDE.md, *"the server may read nothing the form cannot send"* |
| D2 | **Whether a never-reached device should bind to a template is undecided.** Binding on the manifest entry is what makes the approval gate notice its population changed; it also takes a platform's deploy path offline until phase 2 completes. A test pins current behaviour so a change is a decision rather than a discovery. | decide | CLAUDE.md, *"Onboarding revokes its platform's template approval at CREATE"* |
| D9 | **A freshness authorisation has no path a person can use.** `POST /freshness/authorise` requires a verified person: it gated itself since it was written, and now the table does too. No screen calls it. The remedy `nmas-oxidized-freshness` printed on a BLOCKED sync was a `curl` from the NMAS host, which carries no Access assertion and is refused. So the gate's documented way through has never been usable. The message now says what is needed rather than a command that cannot succeed. Found in P.3 step 1, 2026-09-26. | build (a screen: Stage 7.6, freshness authorisations) | `scripts/nmas-oxidized-freshness` |

## E. In the plan, but in no stage

These have acceptance criteria written and no stage owning them.

| # | Finding | Kind | Where |
|---|---|---|---|
| E1 | **Q2 — the Baselines badge and red-line labelling.** Colouring does not match what the entries mean. | build | NSOT_PLAN.md, *Queued from the Stage 2 session* |
| E2 | **Q3 — empty-command rejections at boot.** r3–r5 each logged two rejections of an empty command during the redeploy; the source of the blank line is unfound. Explicitly **not** to be silenced by filtering the log. | build | NSOT_PLAN.md, *Queued from the Stage 2 session* |
| E3 | **Tighten `transport input` on r1–r5.** They still carry `transport input all`. An ordinary authored-intent deploy, deliberately done one device at a time, scheduled after r6's branch site — which is now done. | build | NSOT_PLAN.md, *Deferred* |
| E4 | **Re-enable the drift checker.** Stage 3.3's one remaining item. The known-noisy check that would have fired first — regenerated self-signed certificates — is fixed, so the precondition is met. | verify, then enable | NSOT_PLAN.md, Stage 3.3 |
| E5 | **Authorization: roles and separation of duties are DECIDED and unbuilt.** Any verified person may do every gate kind today. The decision covers the roles, the split of `approve` into `author` and `approve`, per-artifact separation of duties, host-side storage, and the `any_person` default. Two parts are needed before any build: measure whether Access tokens carry groups, and D10's trailer. Stage 7 already draws for it (pattern 6). | build | [NSOT_AUTHORIZATION.md](NSOT_AUTHORIZATION.md); NSOT_PLAN.md *AUTHZ* |

---

## Count

**27 open** (counted from the rows, 2026-09-26): 22 recorded only in prose
(A2–D2, with B13 closed and B15 scheduled, C24 and D4 closed by P.3 step 4, B13 (the terminal leak) added and B14 scheduled, E5 (authorization, decided and unbuilt) added, C24, C25 and D9 added by P.3 step 1, D10 and D11 by step 2 and scheduled by decision, C26 and C27 added and closed, D5 closed by step 3, A4, B8–B12, C6–C10, C23, D4–D8, C12–C14 and C17 added the same day; B4, B5, B6, B7, D3, C11, C15, C16, C18, C19, C20, C21 and C22 added and closed; C1 closed as not reproduced), 5 in the plan without a stage (E1–E5, one of which is Stage 3.3's
tail). The previous figure, 15, was **off by one**: it was produced by
adjusting an earlier count rather than counting rows, and the rows then held
16. C5 is scheduled (6.5); A1 is closed by P.2, and C3 and C4 are closed.

By kind, counted from the Kind column: **17 build**, **3 decide-then-build**, **4 decide**, **1 verify**, **2 operator's steps**. (The previous line said 15 build, which summed to 25 against 24 rows. It was adjusted rather than counted, the same error as the total's.)

None of them blocks Stage 7 — the per-stage scope and the ordering
constraints are in [NSOT_PLAN.md](NSOT_PLAN.md), *Scope of what remains*,
where **Stage 5's count is flagged as a reading of an acceptance paragraph
rather than an enumeration**: it is the one figure there with a different
provenance from the rest, and enumerating Stage 5 is the first item of Stage 5.

**A1 was the one whose absence makes other work untrustworthy** rather than merely incomplete: a mistake in NetBox has nothing
to restore from. It is now P.2, scheduled ahead of Stage 7 for that reason.

C1 was the second such item until it was measured, and the correction is worth
keeping: a finding recorded as a defect became the explanation for the next
confusing output, and that output was correct behaviour. *A pattern that has
been right four times is exactly the one to distrust on the fifth* — the
register makes a finding easier to find, and easier to reach for.

## Scheduled out of the register

| # | Finding | Scheduled | Where |
|---|---|---|---|
| B15 | **A rotation completed on the device, updated the store, and left the BOOT configuration holding the exposed password, with nothing lasting to report it. A timer closed the window.** Measured 2026-09-26 on s1: the startup line hashed identically to the pre-rotation golden (`nmas-check-startup-applies`: NOT SAFE) from 06:21:50Z until the scheduled clab-sync committed at 06:30:12Z. What the code did, read rather than assumed: the persistence chain stopped at `clab_sync` or earlier, so the CLI's state was `ROTATED_UNVERIFIED` and it exited 1. So it did NOT report success. Four things add up to "nothing reporting it": **(1)** that message lived only in a terminal (B2); **(2)** its first words are *"s1: ROTATED and committed"*, the success words, ahead of the danger; **(3)** the stage could never pass on this host, because `clab_sync_script` has been EMPTY since the 2026-09-23 erasure (it is in `GUARD_GATING_EMPTY_DEFAULTS`), while the working timer's unit names the real script (`/home/dmarchak/bin/clab-sync`): **two owners of one fact, and the rotation reads the blank one**; **(4)** nothing watches startup files continuously: `nmas-check-startup-applies` is run by hand. **The operator's acceptance:** after a rotation, `nmas-check-startup-applies` for that device must read SAFE before the rotation reports success; and if a persistence stage does not run, the rotation says which one and does not report success. **Root cause is C28** (the erasure left load-bearing settings empty, and nothing re-established them). The operator set `clab_sync_script` 2026-09-26, and the settings job now reports any such setting while it is empty. The operator's point about item (2) generalises: a message whose first words are good news is read as good news regardless of what follows, as with "1 deployed, 0 not" beside a coverage bug. | 2026-09-26 | **P.3 step 12**: [NSOT_PLAN.md](NSOT_PLAN.md) P.3; the setting is an operator's step now |
| B14 | **Every device stores its login password twice**, as `password` and again as `secret`. Rotation writes it (the CSV branch sets `row["secret"]` to the new password), and so does onboarding's override (`set_device_override(ip, user, pw, pw)`). No device has an enable secret, and Netmiko sends `secret=` only in answer to an enable prompt, so the copy does nothing the password does not, and it is what made the terminal's unconditional send look harmless (B13). The break-glass record's `has_enable_secret` reads the duplicate and claims every device has one. The operator's point: **a credential stored twice is a credential that leaks twice.** | 2026-09-26 | **P.3 step 11**: [NSOT_PLAN.md](NSOT_PLAN.md) P.3 |
| D10 | **Every `Actor:` trailer before P.3 is a CLAIM, and Versions would show it as a fact.** Measured on the host 2026-09-26: of 92 commits in Default's repository, 5 carry a verified identity: two through onboarding's in-route gates (r6's create, and its phase-2 credential rotation) and three made after c5a34c1 was deployed. The rest name `dustin` (33; typed into request bodies by hand during Phase 3, as in the plan's example trailer), `pipeline` (14), `user` (13, the routes' default), `dmarchak@nmas` / `dmarchak` (CLI, the OS user), `operator`, `nmas`, `description-repair`, or nothing (12). **Proposed**: mark by a TRAILER the code writes, not by DATE. The host ran old code after c5a34c1 was pushed until the operator pulled, so a commit's date does not say which code wrote it. New commits would carry `Actor-Verified: access` (a gated route), `host-shell` (a CLI on the host, where SSH is the authentication) or `none`. Versions would show a commit without it as *"recorded, not verified (before P.3)"*: muted, never rewritten. The few pre-P.3 commits made through gates that were already enforced (onboarding, ratify, publish) are marked too. That is conservative, and the label says "not recorded as verified", not "false". **DECIDED 2026-09-26 (operator)**, with an addition: Versions states the COUNT once, *"N of M commits carry a verified identity"* (5 of 92 at the decision: the actor filter answers from claims for 95% of the history), as drift's *"checked 7 of 9"* does. | 2026-09-26 | **P.3 step 10** (the trailer) and **7.5** (the count and the marks): [NSOT_PLAN.md](NSOT_PLAN.md) P.3 |
| D11 | **Template approval is all-or-nothing per platform, and its device-set half duplicates the per-device gate.** At scale, one device the template cannot reproduce blocks deploys to every other device on the platform, and every device joining revokes the approval for all. The argument and a recommended scheme 3 (approval = the template closure hash; fidelity stays per device, live) are in [NSOT_FEATURE_AUDIT.md](NSOT_FEATURE_AUDIT.md) section 8c. It would also resolve D2. **DECIDED 2026-09-26: scheme 3.** The operator's addition: the approval badge says what it covers and what it does not, or scheme 3 reads as weaker than scheme 2. | 2026-09-26 | **P.5** (placement approved: after P.4, before 7.0), badge in 7.6: [NSOT_PLAN.md](NSOT_PLAN.md) |
| B12 | **The identity gate is not enforced on the paths that change devices; CLAUDE.md says it is.** Measured 2026-09-26 by calling each route with NO identity: `/onboard/create` answers **403** ("requires a verified identity"), gated ahead of input validation as documented. But `/deploy/apply` answers **400** ("Nothing confirmed"), `/golden/restore/apply` **400** ("ref is required") and `/ai/approvals/<id>/approve` **404** ("not found"): each reached its own input handling, so no gate ran. `identity.require()` is called ONLY from onboarding (create, verify, abandon, bootstrap reveal), the golden reveal, the remote publish routes and posture ratify. A static scan of the 19 mutating routes that reach a device finds **one** gated (`/onboard/verify`). The other 18 include the deploy and restore confirms, approval, `/execute_command` (config mode), `/run_script`, `/bulk_execute`, `/bulk_reload`, both unguarded golden-restore buttons (D5), `/device/<ip>/restore_backup`, `/configure/apply`, the TFTP file routes and `/ai/chat` (whose `run_playbook_id` replays config even with AI disabled). The socket `terminal_input` has no gate either. **What stood in for the gate is the network**: the Cloudflare tunnel requires the operator's Access login, and port 5000 is firewalled from the LAN (IPv4). So the practical exposure is anything that reaches the app past those, not the internet. **The settings exist, the gate function exists, and the call sites do not**, which is the gate-that-silently-opens shape at the scale of the whole deploy path. It was believed because CLAUDE.md stated it; my own scan's anchor encoded that belief and failed until a behavioural test settled it. Fix shape: one decorator or `before_request` table keyed on endpoint, so a new device-changing route cannot be added without declaring its action. **CORRECTION AND PROGRESS (2026-09-26, P.3 step 1).** The call-site list above omits `/freshness/authorise`, which gates itself (`approve`). Parsed, there are 11 in-view `require()` calls; a grep reports 13 (a docstring naming it, and the golden reveal helper that GET views call). **Step 1 is built**: `modules/route_gates.py` declares all 131 mutating endpoints and the three terminal events, and one `before_request` hook enforces them ahead of validation. An undeclared endpoint is refused at run time, and `tests/test_route_gates.py` fails on an undeclared or ghost entry. Found on the way: (1) `/jenkins/webhook` recorded a CI result from ANY caller, and the pipeline's CI stage consults it. It is now `approve`, and P.4 removes it. (2) `/bulk_download_config`, `/bulk_tftp_download` and `/device/<ip>/download_file` copy files OFF a device to a TFTP server the FORM names. They are now `reveal`. (3) 14 routes recorded `data.get("actor", "user")`, the deploy's golden commit `Actor: pipeline` and Save All `actor="user"`. All are now the verified person (`identity.request_actor()`). Template seeding keeps `actor="nmas"` as a named exemption: no person causes it, and "nmas" is not one of ACTOR_CONVENTION's kinds. **Remaining for B12:** the host measurements (P.3 acceptance 2), the terminal through the tunnel, and step 9. **HOST MEASURED (operator, 2026-09-26)**: a local unauthenticated `POST /deploy/apply` answers 403 `no_header`. Through the tunnel, a real deploy completed and three commits carry `Actor: dustnm@gmail.com` (extraction, template approve, and the deploy golden that said `pipeline` before). The terminal through the tunnel is still unreported. **Step 2 built**: eight direct-push routes removed. | 2026-09-26 | **P.3 steps 1 and 9**: [NSOT_PLAN.md](NSOT_PLAN.md) P.3, or [NSOT_STAGE7_PLAN.md](NSOT_STAGE7_PLAN.md) for a Stage 7 step. |
| D6 | **`/configure/apply`'s docstring promises guards its body does not have.** It lists "Safety check — block genuinely dangerous commands", "Pre-backup" and "Golden config — saved immediately"; the body (app.py:4760-4964) contains none of them. It sends `configure terminal … end` and `write memory`, and triggers Jenkins after the push only when Jenkins is configured. A reader deciding whether the quick path is safe to keep would be told it is guarded. Stage 7's open question on `/configure/apply` must be decided against the code, not the docstring. | 2026-09-26 | **P.3 step 2**: [NSOT_PLAN.md](NSOT_PLAN.md) P.3, or [NSOT_STAGE7_PLAN.md](NSOT_STAGE7_PLAN.md) for a Stage 7 step. |
| C2 | **`routes/templatize.py`'s fleet report drops a device with an unreadable golden through a bare `continue`.** Fourth instance of *the artefact is not the population*; the other three are fixed. | 2026-09-26 | **Stage 7.6**: [NSOT_PLAN.md](NSOT_PLAN.md) P.3, or [NSOT_STAGE7_PLAN.md](NSOT_STAGE7_PLAN.md) for a Stage 7 step. |
| D7 | **Reordering devices fails on a NetBox-sourced list, and CLAUDE.md says it works.** Drag-and-drop calls `/reorder`, which writes the CSV and raises `PermissionError` on a NetBox-sourced list. The client only logs to the console. The route that stores order in `source.json` (`routes/inventory.py` ~70) is called only by `saveInventoryOrder`, which nothing calls. The existing unreachable-UI test passes it, because the function's name appears in its own error string. | 2026-09-26 | **Stage 7.4**: [NSOT_PLAN.md](NSOT_PLAN.md) P.3, or [NSOT_STAGE7_PLAN.md](NSOT_STAGE7_PLAN.md) for a Stage 7 step. |
| D8 | **Four front-end libraries load from CDNs, against the "vendored, never CDN" convention.** `index.html` loads vis-network (unpkg) and SortableJS (jsdelivr); `device.html` loads xterm and socket.io. On an air-gapped network the topology view, drag-and-drop and the terminal fail to load. This matters for the CI decision, which weighs whether the tool must work without the internet. | 2026-09-26 | **Stage 7.3 and 7.4**: [NSOT_PLAN.md](NSOT_PLAN.md) P.3, or [NSOT_STAGE7_PLAN.md](NSOT_STAGE7_PLAN.md) for a Stage 7 step. |
| C5 | The documented deployment is not the one running. | 2026-09-25 | **Corrected, then scheduled.** First recorded as "no unit". The unit exists as `flask-app.service` (enabled, `Restart=always`, journal), but the journal carries only the start-up banner. The log channel is fixed (docs and CLI name and read `logs/device_manager.log`). What remains is hardening, `NMAS_HOST`, and versioning the unit plus `nmas-deploy`: [NSOT_PLAN.md](NSOT_PLAN.md) **6.5**. |

## Closed

| # | Finding | Closed | Reason |
|---|---|---|---|
| B11 | `GET /settings` returned three secrets in cleartext, ungated, since 2026-04-12. | 2026-09-26 | **Code fixed, P.3 step 6:** set/unset flags only, an empty secret field saves nothing, and the modal sends a secret only if typed. A sweep of all 86 argument-free GET routes with planted secrets finds none. **The exposure that already happened is retired only by rotating the Anthropic key**, which is P.3's "do this first" item, reported by the operator as under way and not yet confirmed here. |
| B16 | **`/run_command/<ip>` ran ANY exec-mode command from a GET, with no identity check.** Found 2026-09-26 by surveying the GET routes for step 6: of ten GET views that connect to a device, it was the only one sending text the REQUEST supplied. Exec mode includes reload, delete, copy and clear. The gate table's population was defined by HTTP method, so this route was outside it, and a link followed by an operator logged in to Access would have run it. | 2026-09-26 | **Fixed:** a POST, gated `confirm`; quick actions are buttons carrying their command; a test asserts no GET-only view sends request-supplied text to a device, with a control built from the old body. |
| C29 | Every HTTP error except 404 was answered as a 500 "unexpected error, check the logs", and logged at ERROR. | 2026-09-26 | **Fixed:** `handle_exception` returns an `HTTPException`'s own status and logs it at WARNING. It was found when `/run_command`, now POST-only, answered a GET with 500. |
| C28 | **After the 2026-09-23 settings erasure, nothing re-established which settings were LOAD-BEARING, so each empty one was found by the failure it caused** (the operator's framing: one finding, found four times). `clab_host` was found during r6's persistence, `oxidized_url` during the freshness work, and `clab_sync_script` during a credential exposure, where it extended the leak's boot-file window (B15). **Why the existing check never fired:** `settings_schema.discover_empty_default_guards()` derives the class from the CODE and was called only by tests. It answered *"which settings gate a guard"*, and nothing ever asked *"are they set on THIS install"*: a check of the code, standing in for a check of the deployment. C7's frozen-defaults classifier is adjacent and different; that is about defaults that MOVED, and this is about values that were ERASED, whose absence shows only when the guarded path runs. | 2026-09-26 | **Fixed as a JOB.** `job_health.settings_rows()` gives one row per guard-gating setting (discovered, plus the hand-recorded list): `ok` when set, and `unset_guard` naming what it gates when empty. `nmas-jobs` and `/jobs/health` carry them, which makes them Needs-attention rows in 7.2. An unreadable settings file is one `unknown` row, never a row per key, and a scan that finds nothing is `unknown`. **Its first live finding, measured on the host 2026-09-26:** `yang_push_script` is EMPTY, a fifth instance, which gates the rotation's warning that the yang-push subscriptions use the rotated credential. It is recorded by hand because it names its key in an advisory, so the code scan cannot see it. The other four are now set. The operator's step is to set it; the job keeps it visible until then, so it needs no register row of its own. |
| C23 | The restore preview's "the inventory is the population" fix was in a function no route called. | 2026-09-26 | **Fixed, P.3 step 5.** The rule is in `build_targets()`, which the preview and the apply both call. A scoped restore also names a requested device the ref lacks. `coverage()` gives the inventory denominator, and `plan_restore()` is deleted. Tested on a real repository, including the sentence the operator reads. |
| B13 | The terminal put s1's LOGIN password on screen, and the rotation that would retire it refused. | 2026-09-26 | **Closed on its two conditions, measured.** (1) **Device:** s1 rotated with the ordinary tool after the preflight fix (`abea5ca`). Commit `e1e8e49` at 06:21:50Z; `nmas-check-credential s1 --expect accepted` returned ACCEPTED, so the exposed value is dead on the device. Its boot file has read SAFE since the scheduled sync committed at 06:30:12Z (B15). (2) **Break-glass replaced** (operator): created 06:26:54Z, list Default, 9 devices, complete, key escrowed (fingerprint `f42bddcea15442c2`), sha256 `e0cb9b42…d27b63`. s1's digest changed (`ed11f43aef5a` to `7a70c481de6c`) and the other eight are unchanged, so the new credential is in the record and the exposed one is not. No temporary copy on the NMAS. For the four minutes from 06:26:54 to 06:30:12 the record was correct and the device's boot config was not. **Stage evidence UNRECOVERABLE:** the rotation CLI's stage lines were in a terminal scrollback that no longer exists. That is B2's cost, measured twice in one incident: once for which stage stopped, and once for this closure. Established instead by elimination, from settings that are durable: `clab_sync_script` is EMPTY on the host (the lab map inherits it), so `run_sync` returns *"not configured"*. The chain therefore cannot have passed its `clab_sync` stage; an earlier stage may have failed first, which is unknowable. **The operator's framing is the finding's shape: the leak and the blocked remedy were found in the same minute, and either alone would have been much worse.** The terminal fix, the preflight fix and the page statement are `abea5ca` and `0ca267e`. |
| D4 | The deploy wizard drew the diff, not the program; dangerous lines could not be authorised; attribution was not drawn. | 2026-09-26 | **Fixed, P.3 step 4.** The wizard draws `commands`, with a checkbox per dangerous line that re-plans the device so the hash covers it, and draws the attribution split. Apply sends the authorisation from the rendered plan. Tested end to end against a real plan payload. The restore path can authorise too. |
| C24 | `/deploy/apply` dropped a confirmed device whose artifact could not be built. | 2026-09-26 | **Fixed, P.3 step 4**: a named refusal (*"could not be built at apply: … Nothing was sent."*), counted in the report's total. |
| D5 | **Two GUI buttons push a golden config through the UNGUARDED line-by-line replay.** `POST /device/<ip>/restore_golden_config` (app.py:1687; the device page's fo… | 2026-09-26 | **Fixed, P.3 step 3.** Both buttons open the guarded restore preview at HEAD (`previewBaselineRestore`, the approval handoff's path), and both replay routes answer 404. The AI's tool of the same name is P.3 step 8's. |
| C27 | The restore preview's confirm never showed the lines to be ADDED. | 2026-09-26 | **Fixed, P.3 step 3** (opened and closed the same day). It showed counts and at most three replace and three residue lines, while `commands` was computed, carried to the browser and drawn nowhere, and the confirm hash covered it. It is the restore path's D4. Found by routing D5's buttons into it. `restorePreviewText()` draws every line, and is executed against the route's real payload. |
| C26 | The test suite wrote into the app log of the checkout it ran in. | 2026-09-26 | **Fixed** (opened and closed the same day). Importing `app` attaches a RotatingFileHandler for `logs/device_manager.log` to the root logger, and the harness imports `app` (C20). A suite run in the live checkout on 2026-09-23 left 24 fixture ERRORs in the live app log, the channel `nmas-netbox-modified` counts recorder failures from (C5), where they read as failures of the running app. Found answering C25's live check. `tests/conftest.py` now detaches and closes that handler, and `test_harness_leaves_the_app_log_alone.py` pins it, with a control that the app did attach one. The 24 lines stay in the rotated logs; they are identifiable by their TEST-NET addresses. **What else read it, measured on the host 2026-09-26:** two readers. (1) `nmas-netbox-modified` counts only `modules.netbox_guard` ERROR lines, and the live log holds ZERO of them, so its count was NOT contaminated. The fixture ERRORs came from `modules.nsot.repo`. (2) `/logs/server`, the in-browser tail, derives no number. So the real cost was about 2,600 fixture lines (2026-09-23 21:55-23:50) in a window a PERSON reads, where a fixture refusal looks like a real one. |
| C1 | `nmas-deploy` reported "already current" while the host was behind. | 2026-09-26 | **Closed as NOT REPRODUCED, not on a root cause.** One controlled run, cold, in one block, with GitHub read independently by `ls-remote`: BEFORE `HEAD d01e0b2 origin/main d01e0b2 GitHub 982fb0c`; `nmas-deploy` printed `d01e0b2 -> 982fb0c` with no explicit fetch; AFTER all three `982fb0c`. The CURRENT case was measured the same way the run before (`already at d01e0b2`, true against `ls-remote`). **Recorded explanation (not proven): a race.** All three reports came while this session's commits were being pushed through the host relay, so "already current" was true when printed and false a minute later. **A future sighting is not a new bug by default**: check first whether a push landed after the deploy's fetch. The operator added operand printing to `~/bin/nmas-deploy` (2026-09-26), so any recurrence prints HEAD, origin/main and GitHub, and is self-evident rather than an argument. Two controlled attempts were consumed first, by a deploy and then a pull run before the block: the setup destroying the measurement's precondition. |
| A1 | Nothing backed up NetBox. | 2026-09-26 | **Closed on evidence (P.2).** Hourly `pg_dump` on an exported snapshot, with media, env and configuration, kept plain on the VM, gpg-encrypted to the Proxmox host (rrsync write-only, forced command in two places) and daily to B2. Three copies of 363,799 bytes. Restore test PASS (198 tables, 3,143 rows). The off-box copy was fetched with the read key and decrypted on the laptop. A broken destination is named by `nmas-jobs`. Timers enabled. Pending in the acceptance: the unattended watch, the rrsync refusal half, and a `--status` exit 0 (NSOT_PLAN P.2). Open beside it: B8, B9, B10. |
| C22 | rclone's exit code proved neither permission nor transfer, and the off-box push trusted it. | 2026-09-26 | **Fixed.** Measured by the operator: a refused read was retried ten times (`401`), then rclone printed `There was nothing to transfer` and exited 0, with the 401 visible only at `-vv`. The push path did not read, so it was unaffected on the day. But it confirmed its upload by exit code alone, and so would anything added later. Now every push is confirmed by LISTING `daily/` (a directory listing, which needs only `listFiles`; a single-file path would HEAD and need `readFiles`) and requiring the object at the artefact's exact size. An enabled prune is confirmed by listing too. The real `lsjson` shape was measured on the host before relying on it. Four controls. |
| B7 | `vmdata` became thin-provisioned and nothing watched its real allocation. | 2026-09-25 | **Built and live.** `job_health` reads `disks/zfs` (ALLOC/SIZE, what `zpool list` prints; readable by the auditor token, measured) and reports each pool: `pool_degrading` at 80 % (a convention, named as one), `pool_will_pause` when the headroom above ZFS's write reserve (1/32 of the pool, capped at 128 GiB) falls under 5 % of the pool, and `pool_unhealthy` for any health but ONLINE. An empty answer is `unknown`, never "no pools". Live: `vmdata 8.6% allocated (79.4 GiB of 928.0 GiB), frag 27%, ONLINE; 819.6 GiB before ZFS refuses writes`. Four negative controls, including substituting Proxmox's reservation percentage for ALLOC/SIZE. The thresholds come from OpenZFS and QEMU defaults, not from measuring a pool filling here. |
| B5 | `data/key.key` was a single copy. | 2026-09-25 | **Closed on evidence.** The key is escrowed in the break-glass record (`nmas-breakglass` export with the key; `verify --live` decrypts the stored values with the ESCROWED key). A copy of `data/` exists in B6's nightly image. The two were tied together by the restore test: the fingerprint of `key.key` inside the restored clone equals the escrowed `f42bddcea15442c2`, and the clone's app started on it. |
| B6 | No VM on the Proxmox host was imaged. | 2026-09-25 | **Closed on evidence.** A nightly vzdump of VMs 100 and 102 to a 150 G volume on `sda`, a separate physical disk from `vmdata`, with `is_mountpoint yes`. Both images were taken with the guest agent's fs-freeze. `job_health` watches it through a read-only token: per VM from the task logs, the storage (`inactive` / `will_not_fit` / `images_missing`) and the thin pool. The unmount check read `inactive`. **The test restore passed on the real image**: 9102 up with its links down, postgres recovered, HTTP 200, and the key fingerprint matched the escrow. An image stays on this host: it covers a broken VM and a dead NVMe, not a lost host. Found on the way: B7. |
| C21 | The heartbeat measurement AND the alert rules counted any line that merely named the applet. | 2026-09-25 | **Fixed.** Found because the check correctly reported s4 INSEPARABLE after the alert test. The long gap from the silence (2,925 s) WAS excluded. The cause was a SHORT one: 73 s, ending at `%HA_EM-4-FMPD_NO_EVENT: No event configured for applet NMAS-HEARTBEAT`, which the substring query `|= "NMAS-HEARTBEAT"` counted as a heartbeat. The alert rules use the same query, so a broken applet's own error read as a sign of life: the unsafe direction. Now anchored on the line's exact form, `%HA_EM-\d-LOG: NMAS-HEARTBEAT: NMAS-HEARTBEAT`. Over 6 h of the whole fleet those were the only two shapes naming the applet. **Also:** every gap spanning the device's own `%SYS-5-CONFIG_I` is excluded, because re-entering the applet restarts its timer and leaves a gap between one and two intervals: under the long-gap cut, and enough to make any maintenance window leave INSEPARABLE behind it. Re-measured live: all nine devices `measured`, s4 at window 1005 s. **The generated rules file was an untracked file in the live checkout** (the script's default output path), so committing it would have aborted the next pull. It is now gitignored, and `--check`'s help no longer calls it "committed". **Operator step:** regenerate and reinstall the rules; the installed ones still carry the substring query. |
| C20 | A settings-route test failed intermittently, echoing `kea_username` as `''`. | 2026-09-25 | **Two causes, both measured and fixed, neither the one first suspected.** **(1) The test failure: import-time binding.** `modules.integrations.base` binds `get_setting` by name. When `test_onboard_plan.py` ran before anything imported `app`, `base` was first imported inside that file's `lab` fixture, while `settings_schema.get_setting` was a lambda answering unlisted keys with the schema default. `base` kept the lambda. An import hook recorded the import, on the main thread, with the lambda bound. The order was deterministic: 3 of 3 failed with that file first, 2 of 2 passed with it second. The **streaks were my subset command**: `grep` in the tool shell is a parallel `ugrep` and returns files in varying order. `conftest.py` now imports `app` before any test runs, as the program does, and a subprocess test runs the failing order. **(2) Found while chasing it, and worse: concurrent settings writes CORRUPTED the file.** A probe recorded every write during a failing run and saw only the route's own two, both correct, so the redaction refresh and the background threads were ruled out by measurement. But `save_user_settings` used one temp name for every writer, and no read-modify-write held a lock. Two threads × 150 writes left `user_settings.json` unreadable in 2 of 2 runs, and the unreadable-file guard then refused every write. Fixed: a per-write `mkstemp`, and `config.settings_lock()` (an RLock, plus `flock` across processes) around all four read-modify-write sites. After the fix: 300 of 300 keys, threads and processes, 3 of 3 runs. An AST scan requires the lock in any function that both loads and saves. The general-settings POST was one of the four, and it **bypasses `write_settings()`**, so CLAUDE.md's *one write path* was not true. |
| B4 | NetBox's env files were world-readable (0664). | 2026-09-25 | **Closed on evidence.** The operator ran `chmod 600`; measured later that evening, all four `env/*.env` read `600` and `netbox-docker-netbox-1` was `Up 6 hours (healthy)`. NetBox also answered the heartbeat generator's queries after the change. The backup writes its copies `0600` regardless (pinned). |
| C18 | Commits from a CLI never pushed. | 2026-09-25 | **Fixed.** Post-commit hooks run on daemon threads, and a short-lived process (`nmas-retire`) exited under its own push: r5's retire commit `3592113` never reached `rcn-nsot-config`, and nothing recorded a failure. Hook runners are now joined at exit, bounded at 90 s; an unfinished one is named on stderr as "the commit is local and may not have been pushed". Proven in a real subprocess, with a control. `3592113` pushes with the next commit from the app, or by hand. |
| C19 | Secret stores the checker could not see, and their modes. | 2026-09-25 | **Fixed in code.** `nmas-check-secret-storage` classifies EVERY file under `data/` (secret: must be 0600 / no-secret: scanned / config-bearing / plain), and an undeclared file fails the run, so a new store is noticed on its first run. Outside `data/` it checks a declared list (rclone.conf, the NetBox env files, the backup push key). Live: 773 files classified, 0 unclassified, and **5 loose secret stores found**: `credential_profiles.json`, `devices.csv` plus two old copies, and `secret.key` (Flask's session key), all 0664, beneath a run that had printed "every file holding a secret is owner-only". The credential store's and devices.csv's writers and `secret.key`'s creation now go through `open_secure`, so the next write heals them. The two old CSV copies and `secret.key` are never rewritten, so they need `chmod 600` by hand. Actual exposure was limited: `data/` is 0700. |
| C16 | The per-platform heartbeat window was the wrong shape. | 2026-09-25 | **Built: per-device windows** (`scripts/nmas-heartbeat-rules`). Each device's window is the midpoint of its own separable band, measured from its Loki arrivals, with misses in the history excluded. A new device gets a **provisional** window, labelled as such in the rule itself and derived from the fleet's slowest measured rate; a device with an empty band is named `inseparable`. `--check` re-measures hourly on its own timer, declared in `job_health`, and names `STALE RATE`, `PROVISIONAL READY`, `UNMEASURABLE`, `MISSING` and `EXTRA`. **Live: nine measured windows**, r1–r4 and r6 750 s, s1 797, s2 799, s3 1286, s4 996; r5 named not expected; `--check` current for all nine. Controls shown failing. |
| C15 | A write whose commit failed was stranded, and the run that stranded it reported success. | 2026-09-25 | **Fixed in `scripts/oxidized-to-config.sh`.** Every run reconciles every managed file BEFORE the early exit and before any copy. A dirty file is committed ("recorded late") only when it is byte-identical to `render_device`'s output for the Oxidized version its own header names; anything else is REFUSED for that device, neither committed nor overwritten, named, and the run exits 3 (including on the "nothing to copy" path). The per-lab commit now adds and commits only the job's own files by path. The executed tests found their own fixture could not exhibit the version case (its second poll changed only a stripped comment); fixed, and the control then fired. r6's stranded file was committed by hand by the operator. |
| C11 | Deleting a device left it half-managed in five places; there was no retire. | 2026-09-25 | **Built: `scripts/nmas-retire` / `modules/nsot/retire.py`.** Clear the override, declare the startup file deliberately unmapped, then one commit that removes intent and golden and releases the identity (history kept), then the CSV row LAST, and only against a break-glass record holding the device's CURRENT credential (a refusal, not a step). It states every time what it will NOT do: NetBox kept, Oxidized polling, startup frozen, device config unchanged, backups kept, the pooled session. It is resumable, and a failed commit puts the tree back. **ACCEPTANCE PASSED 2026-09-25 on the real device (operator):** commit `3592113`; zero r5 in host_vars, golden, devices.csv and the manifest; the approval withdrawn naming its cause ("device 'r5' is no longer bound"); `cisco_iosxe/base.j2` binds r1, r2, r3, r4, r6; credential overrides: 1 (r6's), 0 unclaimed. Declared-unmapped on its first real use: `produced : 9`, `declared : 1 — r5: ISP device outside our administrative boundary… (dmarchak, 2026-09-25T20:33:27Z)`. Found beside it and fixed: `nmas-clab-targets --device` unpacked 3 columns from 6-column rows and crashed on the first. |
| D3 | The template editor's confirmations did not say what happened. | 2026-09-25 | **Fixed.** The save message is built from the route's `approval_revoked` and `revoked`; the approval cell draws the reason and then what re-approval needs; `_common.j2` is listed as a shared file (Edit only, with what editing it revokes). Three pure renderers are executed in duktape (`test_template_library_renders.py`), with controls shown failing. The operator's step 3 had already shown the underlying state was right; only the panel was not. |
| C4 | `health()` is per-process. | 2026-09-25 | **Decided and built.** The counters stay in-process; `health()` now carries `pid` and `counting_since` (when counting began), and the CLI prints both beside the counts. Durable counts come from `logs/device_manager.log`, which the CLI reads with a denominator. |
| C3 | *"The recorder missed a confirmed write."* | 2026-09-25 | **Measured false.** The record holds the entry (`r1 comments … TEST → …`, `06:34:40Z`), the file's mtime is 0.18 s after NetBox's `last_updated`, the reader lists it, and the app log holds 0 recorder ERROR lines in 549,266. The positive control **passed**; it was read as failing. The misreading's mechanism is undetermined — the entry is the newest of 44, printed last, so any view of the output's head would omit it, but that is a candidate, not a measurement. [PHASE2_DHCP.md](PHASE2_DHCP.md) §23. |
